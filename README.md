cat > README.md <<'EOF'
# Twin Battery Project 🔋

A Flask + ML web application that predicts or monitors battery parameters.

## Setup
1. Create a virtual environment.
2. Install requirements:
   ```bash
   pip install -r requirements.txt

